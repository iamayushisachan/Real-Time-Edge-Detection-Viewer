dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPO)
    repositories {
        google()
        mavenCentral()
        jcenter()
    }
}
rootProject.name = "CompVis"
include ':app'
include(':sdk')
